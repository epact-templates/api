# {{ name }}

> App created by template {{ templateName }} with Epact

{{ description }}

## Build Setup

``` bash
# install dependencies
{{ pkgManager }} install

# serve with hot reload at localhost
{{ pkgManager }} dev

# build project
{{ pkgManager }} build

# start project
{{ pkgManager }} start
```
